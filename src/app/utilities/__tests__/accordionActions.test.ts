import { ACCORDION_TRACKING_DISABLED } from '@/app/utilities/accordionActions';

describe('accordionActions', () => {
  it('ACCORDION_TRACKING_DISABLED is an empty string', () => {
    expect(ACCORDION_TRACKING_DISABLED).toBe('');
  });
});
