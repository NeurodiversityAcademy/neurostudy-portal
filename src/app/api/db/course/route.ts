import GET from './GET';
import POST from './POST';
import PUT from './PUT';

export { /* @next-codemod-error `GET` export is re-exported. Check if this component uses `params` or `searchParams`*/
GET };
export { /* @next-codemod-error `POST` export is re-exported. Check if this component uses `params` or `searchParams`*/
POST };
export { /* @next-codemod-error `PUT` export is re-exported. Check if this component uses `params` or `searchParams`*/
PUT };
